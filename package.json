{
  "name": "mealplanner-saas",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev": "pnpm --filter @mealplanner/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "pnpm -r format",
    "type-check": "pnpm -r type-check",
    "generate-types": "bash scripts/generate-types.sh"
  }
}
